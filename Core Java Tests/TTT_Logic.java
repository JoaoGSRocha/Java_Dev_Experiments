public class RPS_Logic {
		OponentMoves{
		b = sc.nextByte();
		byte op =rb.getRandB();
		byte op_move = 0;		
		if(op >0)
		{ 
			if(op>55) op_move=0;
			else 			op_move=1;
		}
		else{
			op_move=2;
		}	
	}
}
